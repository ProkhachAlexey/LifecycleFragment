<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/uuidTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:textColor="@android:color/black"
            tools:text="123-123-123-123"/>

        <TextView
            android:id="@+id/chuckNorrisFactTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="16sp"
            android:lines="3"
            android:textColor="@color/black"
            tools:text="Some random fact"/>

        <Button
            android:id="@+id/changeUuidButton"
            style="@style/DefaultButton"
            android:layout_marginTop="6dp"
            android:text="@string/change_uuid"/>

        <Button
            android:id="@+id/changeChuckNorrisFactButton"
            style="@style/DefaultButton"
            android:text="@string/next_fact"/>

        <Button
            android:id="@+id/changeBackgroundButton"
            style="@style/DefaultButton"
            android:text="@string/change_background"/>

        <Button
            android:id="@+id/launchNextButton"
            style="@style/DefaultButton"
            android:text="@string/launch_next"/>

        <TextView
            android:id="@+id/numberTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textSize="16sp"
            tools:text="Number: #1"/>



    </LinearLayout>

</ScrollView>